import React from "react";
import { withStyles } from "@material-ui/core";

const styles = (theme) => ({});

const SeasonViewPage = ({ classes, ...props }) => {
  return <div>This is the season's view page</div>;
};

export default withStyles(styles)(SeasonViewPage);
